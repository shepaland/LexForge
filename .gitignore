# Артефакты процесса разработки — нужны в сессии, не пользователю продукта
docs/superpowers/
.superpowers/

# Changes в работе — процесс. Спеки в lexforge/specs/ коммитятся: они описывают
# поведение выпущенного продукта.
lexforge/changes/

# Секреты
.env
.env.*
*.pem
*.key
.npmrc
.claude/settings.local.json

# Сборка и зависимости
node_modules/
dist/
*.tsbuildinfo

# Следы рантайма — остаются локальными, не часть репозитория продукта
.claude/skills/
.claude/lexforge-install.json

# Изолированные рабочие деревья сессий
.claude/worktrees/

# Локальное окружение
.DS_Store
.idea/
.vscode/

# Заметка для старта сессии — процесс, не продукт
NEXT-SESSION.md
