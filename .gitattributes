# Рабочее дерево разворачивается одинаково на всех системах. Иначе тесты,
# читающие файлы репозитория, отвечают по-разному на Windows и на Linux,
# а побайтное сравнение архива пакета с репозиторием расходится на каждой строке.
* text=auto eol=lf
