## Purpose

Shows the shape of a delta spec without declaring anything of its own.

## ADDED Requirements

A delta spec is written like this:

```markdown
### Requirement: Example name

The system SHALL do the thing.

#### Scenario: Example scenario

- **WHEN** a condition holds
- **THEN** a result follows
```

Everything inside the fence is an example, not a requirement.
