---
name: three-fields
description: Use when a structure check needs a frontmatter with a field that does not belong there
version: 1
---

Read the state of the change first: `lexforge status --change <name> --json`.

Next step: `lexforge instructions specs --change <name>`.
